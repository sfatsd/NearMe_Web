export const API_ROOT = 'https://around-75015.appspot.com';
export const TOKEN_KEY = 'TOKEN_KEY';
export const POS_KEY = 'POS';
export const GEO_OPTIONS = {
    enableHighAccuracy: false, // remove if it takes too long to load
    maximumAge: 3600000,
    timeout: 30000,
};